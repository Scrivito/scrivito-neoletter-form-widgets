import * as React from "react";

export const FormNoTenant = ({}) => {
  return (
    <span className="scrivito-form-widgets missing-id">
      Warning! instanceId has not been configured for the form widget.
    </span>
  );
};
